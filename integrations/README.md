# Integrations

This directory contains adapters for external ecosystems rather than codecs or
core Open4D functionality.

- `open3d` converts decoded Open4D frames to Open3D geometry.
- `unity` provides the existing native decoder backend and Unity playback files.

Each integration documents and manages its own optional dependencies.
