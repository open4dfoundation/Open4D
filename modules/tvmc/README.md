# TVMC Module

Time-Varying Mesh Compression (TVMC).

This module implements the TVMC algorithm on top of Open4D core data structures.
Paper-specific reproduction scripts should live in `benchmarks/`.
