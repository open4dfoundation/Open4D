# Depth-prior generation only, in its own environment (open4d-gs-midas).
#
# timm==0.6.13 is QUEEN's pin, needed by MiDaS's dpt_beit_large_512 backbone, and
# it is a 2023 release. Keeping it here rather than in requirements.txt is what
# lets the training environment track current packages: `gs-tools depth-prior`
# writes depth maps to disk, and training reads the cached maps.
#
# QUEEN also ships scripts/patch_timm.py, which rewrites timm 0.6.13's
# maxxvit.py for compatibility. scripts/setup.sh runs it against this
# environment.

torch==2.7.0
torchvision==0.22.0
timm==0.6.13
numpy
opencv-python-headless
pillow
