# qndf had no dependency file at all. These are its direct imports; versions come
# from the repository-wide set in ../../../requirements-codecs.txt, which is the
# file to install.
numpy==1.26.4
open3d==0.19.0
torch==2.7.0
trimesh>=4.1
tqdm
dahuffman
mlflow
